Update the existing "VidLab" Replit project with the following improvements, fixes, and new features, without creating a new project from scratch. Keep the original file structure and frontend intact, but modify backend routes and frontend logic where needed.

====================
1. DUPLICATE VIDEO (Fixes)
====================
- "Every frame" extraction should use fps_extract=0 as sentinel
- Use FFmpeg -vsync 0 for actual frame extraction
- Limit maximum extracted frames to 3000
- Pre-process disk cleanup before extraction
- Remove temporary frames after ZIP creation
- Catch TimeoutExpired and disk quota errors → return user-friendly JSON

====================
2. MODIFY VIDEO (Fixes)
====================
- Pre-process cleanup before encoding
- Catch TimeoutExpired and OSError (No space left) → friendly error
- Remove partial output files on failure
- Fix redundant codec copy logic
- Maintain safe free-tier limits (no disk quota crash)

====================
3. NEW FEATURE: SCENE PROMPTS
====================
- Add POST /analyze-scenes {"file_id": "..."} route
- Extract frames (fps=1 recommended, configurable)
- Use open-source BLIP-2 (Salesforce/blip2-opt-2.7b) locally to generate captions/prompts per frame
- Aggregate consecutive similar frames → 1 prompt per scene
- Return JSON: {scene_number: {frame_list: [...], prompt: "..."}}
- Frontend: add a new tab [Scene Prompts], show scene thumbnails + prompts, download JSON button
- Limit frames for BLIP-2 to 50 per video → safe memory usage

====================
4. FRONTEND FIXES
====================
- Update FPS dropdown: "Every Frame" now sends fps_extract=0
- Remove old special-case 99999 fps workaround
- Scene Prompts tab → independent display
- Show typewriter-style progress messages for scene prompt generation
- Ensure download buttons for frames, clean video, modified video, and scene prompts JSON work

====================
5. FREE-TIER / REPLIT SAFETY
====================
- Background cleanup thread → delete /tmp files older than 1 hour every 30 minutes
- Pre-process cleanup for all FFmpeg operations
- Limit extracted frames for every frame extraction (max 3000)
- Catch disk quota, timeout, and memory errors with friendly JSON messages
- Remove temporary files after processing

====================
6. DEPENDENCIES / REPLIT CONFIG
====================
- Keep Python 3.11 + Flask
- Add torch, transformers, Pillow for BLIP-2 scene prompts
- Nix packages: ffmpeg, git, wget
- requirements.txt update: flask==3.0.3, torch, transformers, Pillow
- Keep original .replit run commands

====================
7. DOCUMENTATION / COMMENTS
====================
- Clearly comment new routes (duplicate, modify, analyze-scenes)
- Document free-tier limits and BLIP-2 usage
- Log errors with timestamps for debugging
- Maintain all previous frontend features (analysis, duplicate, modify)

====================
IMPORTANT NOTES
====================
- Do not create a new project; update the existing one
- Ensure all previous functionality works
- Fully free, zero API keys, zero paid services
- Compatible with Replit free tier and safe for daily usage